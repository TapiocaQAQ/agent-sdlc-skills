# agent-sdlc 預設 SDLC 流程(SOP)

這是 pack 的**預設完整 SDLC 流程**,忠實對應 appleboy(Bo-Yi Wu, MediaTek)Cloud Summit 2026 的 Agent-Skill SDLC 鏈。**不自行簡化關卡。**

用法:每個 PR 開一支進度檔(見 `plugins/agent-sdlc/templates/sdlc-progress.template.md`),由 `/agent-sdlc:sdlc` 導航——它讀進度檔 + git 狀態,更新勾選並回報「📍目前位置 / ⏭下一步 / 卡點」。你只要照著下一步做;每過一個 pack 關卡,該關 skill 會自動回呼 `sdlc` 更新進度。

## 12 步 master table

| # | 關卡 | 類型 | 範圍 |
|---|---|---|---|
| 0 | prompt-audit — 需求把關 | pack skill | **feature 層一次** |
| 1 | 規劃(雙軌 superpowers/plan-feature)→ 產 plan/spec | 規劃 | 每 PR |
| 2 | 🚦 planning-exit checklist | 人審閘 | 每 PR |
| 3 | classify-change — leaf/core 校準 | pack skill | 每 PR |
| 4 | 寫 code | 實作 | 每 PR |
| 5 | /simplify | 內建 | 每 PR |
| 6 | /security-review | 內建 | 每 PR |
| 7 | /code-review max -fix | 內建 | 每 PR |
| 8 | 🚦 commit-message(人審 + Co-Authored-By) | pack skill / 人審閘 | 每 PR |
| 9 | pr-prepare(第二檢查點 + 資料紅線) | pack skill | 每 PR |
| 10 | ⛔ external-ai-review(去敏 + /loop 收斂) | pack skill / 去敏閘 | 每 PR |
| Δ | **枚舉這一輪自己改過的每一行** | pack skill(`delta-enumerate`) | 每 PR |
| 11 | 🚦 人審合併(core 逐行;push/PR/merge 核可) | 人審閘 | 每 PR |

- **來源**:第 0/1/3/8/9/10 關對映本 pack 的 skill,第 5/6/7 是 Claude Code 內建,第 2 關用 pack README 的檢查表,第 11 關是本地規範。
  **Δ 是本地新增的一關,上游 appleboy 沒有**——2026-08-10 起它也有自己的 skill(`delta-enumerate`)。
- 🚦 **人審閘 ×3**:第 2、8、11(其中 8、11 含核可動作)。⚠️ 舊版本檔寫 ×4 但只列了三關,是誤植。
- ⛔ **去敏閘 ×1**:第 10。
- **內建關卡 ×3**:第 5、6、7(Claude Code 內建、非本 pack,**無法自回呼**;由**第一個跑到的 pack 關卡**追認——依下方執行順序即第 9 關 pr-prepare)。
  - ⚠️ 有些環境把 `/code-review` 設成 `disable-model-invocation`——**agent 叫不動,必須由人手打**。若第 7 關卡住,先確認是不是這個,別當成流程錯。

### gate 0 是 feature 層,不是 PR 層

`prompt-audit` 把的是**需求輸入端**。一個 feature 拆成多個 PR 時,需求在 feature 開題時就把過了,
**每個 PR 不重跑**——開工時只重驗六要素有沒有落在這個 PR 的那一節規劃上。
(三輪實測都是這樣做的,但每次都要臨場論證一次「不重跑」;寫在這裡就不用再論證。)
⚠️ **scope 有實質變動就回頭重跑 gate 0/1**——實測有一次 PR 開工重跑規劃關,
用原始碼查證出兩條會改實作的前提,那是規劃階段沒查的。

### 第 5/6 關的結論在第 7 關跑完前**不給定案編號**

第 7 關是這條鏈裡唯一有裁決權的自動關卡。實測它**推翻過第 5 關與第 6 關各一條已經報告完的結論**,
兩條都是同一種病:**把一次量測當成規格**。

所以 5/6 的發現先記成「待第 7 關確認」,第 7 關過了才編定案號、才寫進 plan 的決策區。
零成本,但擋掉「錯的結論被鎖進 plan、後面每一關都拿它當前提」。

⛔ **不要因此砍掉第 5/6 關**:它們的 mandate 和第 7 關不同(`/simplify` 明文不找 correctness bug),
實測第 5 關抓到的東西第 7 關一條都沒抓到。

## 執行順序:`… → 7 → 9 → 10 → Δ → 8 → 11`

⚠️ **編號是名字,不是順序。** 實際執行順序把第 8 關(commit)往後挪,並在它前面插入 Δ:

```
0 → 1 → 2 → 3 → 4 → 5 → 6 → 7 → 9 → 10 → Δ → 8 → 11
```

**為什麼 8 排到 9/10 後面**:`external-ai-review` 審的是 **local diff**。先 commit 再送審,
等於把「審出來的修正」堆成第二顆 commit,或者要 amend。commit 排最後,一顆 commit 就是收斂後的結果。
(上游 appleboy 的順序 8 → 9 → 10 成立,是因為他那兩關是 **GitHub PR bot**,本來就需要先有 commit 和 PR。
本 pack 的 reviewer 是同步的 local reviewer,前提不同,所以順序不同。)

⛔ **Δ 排錯位置的代價是實測過的**:有一輪 Δ 補做在第 8 關**之後**,結果整條尾巴重跑一遍——
第 7 關 ×2、Δ ×2、commit ×3 顆。照順序跑的那一輪是第 7 關 ×1、Δ+Δ′、commit ×1 顆。
**這是三輪實測裡最大的一筆浪費,而且原因純粹是順序。**

### Δ:枚舉這一輪自己改過的每一行

**這一關存在的理由**,一句話:**抽樣式審查(外部 AI、人工 review、隨機探測)天生找不到
「你剛剛修的那條修得不夠寬」**——因為那看起來已經修過了,沒有人會回頭再抽同一個位置。
但那其實是**命中率最高的地方**:寫修正的人剛剛才證明自己在這條軸上想得不夠寬。

| | 內容 |
|:--|:--|
| **母體** | 第 5/6/7/9/10 關**這一輪改過的每一行**,不是整個 diff。**用 git diff 導出,不憑記憶** |
| **方法** | 對每個決策點**枚舉兄弟值**(相鄰拼法、邊界值、同軸的其他成員),不是再抽樣一輪 |
| **未達門檻的當場修,達門檻的不修** | 修完再跑一趟 **Δ′**,母體只有「Δ 自己改的那幾行」,**最多兩趟**——上界是常數,所以會終止 |
| **升級門檻(按性質不按數量)** | ≥1 條「**會實際走到危險路徑,或推翻某條已經寫下來的保證**」→ 升級成完整的第二次 gate 7,**Δ 不准自己修**。nit 不論幾條都不算 |
| **跳過條件** | 第 9/10 關**零採納** ⇒ 母體是空的 ⇒ 跳過,並在進度檔寫明「Δ 空,已跳過」。**⛔ 有採納就是強制,沒有「若需要」這個選項** |

完整做法、可複驗的母體導出指令、實測案例、以及 Fagan Follow-up 的對應,
**都在 `/agent-sdlc:delta-enumerate` 這支 skill 裡**——跑到那一關時才載入,不佔本檔。

> 📌 **Δ 曾經是全鏈唯一沒有 skill 的關,因此是唯一不會自動回呼的關,兩次都出事**
> (一次補做在 gate 8 之後、一次差點沒跑)。2026-08-10 把它做成 pack skill,
> 它現在跟其他 6 支一樣自己回呼導航器。**這是修那兩次的根因,不是加規則。**
>
> ⛔ 順帶記一條判讀紀律:「我自己更新進度檔」**不等於**「跑過導航器」。
> 進度檔是導航器的**產物**;自己寫產物就等於把要驗算的答案自己填上去。
> 實測那次的 ⏭ 就是這樣變成憑記憶寫的「Δ(若需要)」——**檢查一下自己有沒有在複述規則時偷偷加軟化詞。**

## 導航機制

- **顯式**:隨時打 `/agent-sdlc:sdlc` 問「我到哪了、下一步是什麼」。
- **自動**:在跑完整流程時(進度檔存在),每個 pack skill(第 0/1/3/8/9/10/Δ 關對映的 **7 支**)收尾會回呼 `/agent-sdlc:sdlc`,自動勾選本關、刷新下一步;單獨用一支 skill 則不回呼、只在該支收尾提醒下一步。
- **進度檔**:**一個 PR 一支**,放開發 repo `docs/sdlc/<feature>-<pr>-sdlc-progress.md`、**gitignored working scratch**(同 mem-tmp 性質)、**該 PR merge 即刪不歸檔**——commit + PR 本身就是永久紀錄。
  - ⛔ **不要一個 feature 共用一支。** 實測一支 4-PR 的共用進度檔長到 **845 行 / 80 KB**,
    而導航器**每一關都要讀它一次**;第一個 PR 早就合併了,它的段落還躺在裡面被反覆讀。
  - 跨 PR 才需要的東西(feature 層的 gate 0 產出、跨 PR 決策、還沒關的項目)留在 **plan**,
    進度檔只留這個 PR 的關卡狀態,指回 plan。
